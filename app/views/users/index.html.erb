<h1>Listing users</h1>

<table>
  <thead>
    <tr>
      <th></th>
      <th></th>
      <th></th>
    </tr>
  </thead>

  <tbody>
    <% @users.each do |user| %>
      <tr>
        <td><%= user.name %></td>
        <td>
          <% user.transactions.each do |trans| %>
            <%= trans.product %>
            <%= trans.price %>
          <% end %>
        </td>
        <td></td>
      </tr>
  </tbody>
    <% end %>
</table>

<br>

<%= link_to 'New Transaction', new_transaction_path %>

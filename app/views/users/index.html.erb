<h1>Listing users</h1>

<table>
  <thead>
    <tr>
      <th></th>
      <th></th>
      <th></th>
    </tr>
  </thead>

  <tbody>
    <% @users.each do |user| %>
      <tr>
        <td><%= user.name %></td>
        <td>
          <% user.transactions.each do |trans| %>
            <%= trans.product %>
            <% unless trans.price.nil? %>
              $<%= trans.price / 100 %>.00
            <% end %>
          <% end %>
        </td>
        <td></td>
      </tr>
  </tbody>
    <% end %>
</table>

<br>

<%= link_to 'New Transaction', new_transaction_path %>
